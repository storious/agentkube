# Starters

Small setups that users can copy and customize live here.

`self-maintainer` composes the coordinator and Web Search Plugin with all four
prepared specialists, a directly usable local pool, and an optional
ChatGPT-account pool. Its
`.agents/package.json` references local packages and Plugins; `.agents/pools.json`
is merged into the prepared runtime registry by Agulater.
