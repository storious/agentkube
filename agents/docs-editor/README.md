# Docs Editor

`docs-editor` makes bounded documentation-only changes that remain consistent
with the implementation and surrounding documentation style.

The installable Agulater package is in [`.agents`](.agents/).
