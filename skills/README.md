# Skills

Reusable model instructions live under `.agents/skills/`. Install one from the
registered AgentKube Catalog without copying the repository:

```console
agulater add agentkube:grilling --user
```

`grill-me` is the compatibility package; `grilling` is the maintained Skill.
